import React, { FC } from 'react'
import { observer } from 'mobx-react'

import FullTab from '../components/full_tab'

const ProductionTask: FC = observer(() => {
  return <FullTab />
})

export default ProductionTask
